# Code generated by pocket. DO NOT EDIT.
# Run `./pok github-workflows` to regenerate.
# See: https://github.com/fredrikaverpil/pocket

name: pocket

on:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go-fix-ubuntu:
    name: go-fix (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-fix-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-fix-ubuntu-
      - name: Run go-fix
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-fix
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-fix-ubuntu-${{ hashFiles('**/go.sum') }}

  go-format-ubuntu:
    name: go-format (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-format-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-format-ubuntu-
      - name: Run go-format
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-format
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-format-ubuntu-${{ hashFiles('**/go.sum') }}

  go-lint-ubuntu:
    name: go-lint (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-lint-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-lint-ubuntu-
      - name: Run go-lint
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-lint
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-lint-ubuntu-${{ hashFiles('**/go.sum') }}

  go-test-ubuntu:
    name: go-test (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-test-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-test-ubuntu-
      - name: Run go-test
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-test
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-test-ubuntu-${{ hashFiles('**/go.sum') }}

  go-vulncheck-ubuntu:
    name: go-vulncheck (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-vulncheck-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-vulncheck-ubuntu-
      - name: Run go-vulncheck
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-vulncheck
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-vulncheck-ubuntu-${{ hashFiles('**/go.sum') }}

  md-format-ubuntu:
    name: md-format (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-md-format-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-md-format-ubuntu-
      - name: Run md-format
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c md-format
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-md-format-ubuntu-${{ hashFiles('**/go.sum') }}

  github-workflows-ubuntu:
    name: github-workflows (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-github-workflows-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-github-workflows-ubuntu-
      - name: Run github-workflows
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c github-workflows
      - name: Save cache
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-github-workflows-ubuntu-${{ hashFiles('**/go.sum') }}

